---
language: python
python:
  - "2.7"
  - "3.7"

services:
  - docker

addons:
  apt:
    packages:
    - python-pip

install:
  - pip install ansible molecule[docker]

script:
  - molecule test

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
